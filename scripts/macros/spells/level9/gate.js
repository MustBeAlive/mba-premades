import {mba} from "../../../helperFunctions.js";

// To do: plane images; animation from animation folder; icon;

export async function gate({ speaker, actor, token, character, item, args, scope, workflow }) {

}